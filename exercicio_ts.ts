const multiplicacao = (primeiro:number, segundo:number):number => primeiro*segundo;
const saudacao = (nome:string):string => "Olá "+ nome;
const usuario = saudacao("Charlie");
const result = multiplicacao(5,4);
console.log(result)
console.log(usuario)
